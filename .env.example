# Frontend (Vite — exposed to the browser, MUST be prefixed with VITE_)
# Set these in Vercel → Settings → Environment Variables for all environments.

# Google Gemini API key — used for AI-assisted BRD generation
VITE_API_KEY=

# Default contact / admin email shown in offline banner and forms
VITE_ADMIN_EMAIL=mohammed.alajmi@ohb.co.om
